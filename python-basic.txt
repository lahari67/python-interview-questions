What is a dynamically typed language?
Is Python a compiled language or an interpreted language?
What is the difference between a Mutable datatype and an Immutable data type?
What is the difference between == and is in Python?
What are lists and tuples? What is the key difference between the two?
What is the difference between a Set and Dictionary?
How is a dictionary different from a list?
What is the difference between Python Arrays and Lists?
what is the use of self in python ?
what is the use of__init__ ?
What is break, continue and pass in Python?
What is docstring in Python?
What is the difference between remove(), pop(), and del?
How can you concatenate two lists in Python?
What is the difference between append() and extend()?
What is the purpose of if __name__ == "__main__" in Python?
What is slicing in Python?
What is the difference between a shallow copy and a deep copy?
What is the purpose of the super() function in Python?
What are Magic (Dunder) Methods in Python?
What are Decorators?
How is memory management done in Python?
How to delete a file using Python?
What is the use of help() and dir() functions?
Are access specifiers used in python?
How will you check if a class is a child of another class or instance of class?
How do you create and use a virtual environment?
What are Access Specifiers in Python?
What is the difference between @classmethod, @staticmethod and instance methods in Python?
What are Pickling and Unpickling?
What is a context manager in Python?
What is the difference between dict.get() and indexing dict[key]?

What is a dynamically typed language?
        A dynamically typed language is a programming language in which the data type of a variable is determined at runtime. In Python, you do not need to declare the data            type of a variable explicitly. Instead, Python automatically assigns the appropriate type based on the value stored in the variable.
         Example:
          x = 10       # x is an integer
          x = "Hello"  # Now x is a string
        Here, the variable x initially stores an integer and is later reassigned to a string. Python automatically updates the variable's type at runtime.

Is Python a compiled language or an interpreted language?
        Python is considered as compiled and an interpreted language. First python code is compiled into  byte code(.pyc files) then pvm interprets and executes line by line.
          
What is the difference between a Mutable datatype and an Immutable data type?
        Mutable data types 
                objects whose values can be changed after they are created. Any modification is made in place without creating a new object. Common examples                                 include lists, dictionaries and sets.
        Immutable data types 
                objects whose values cannot be changed after creation. If you modify them, Python creates a new object instead. Common examples include strings, tuples,                     integers and floats.
      
        # Mutable example
        a = [1, 2, 3]
        a.append(4)
        print(a)        
        
        # Immutable example
        s = "Hello"
        s = s + " World"
        print(s)

What is the difference between == and is in Python?
        both is and == are comparision operators. but they differ in their own way.
        == checks the value of an object where as 
        is in checks they refer the same object  memory or not 

What are lists and tuples? What is the key difference between the two?
        Both list and tuples are sequence data types and ordered that can store collection of objects.
        but the key difference is lists are mutable where as tuples are immutable. 
        Means lists once created they can be modified, sliced and appended where as tuples cannot be modified once created.
        Note:
        these contain duplicate elements aswell.
What is the difference between a Set and Dictionary ?
        Set is a unordered collection of sequence data type and is iterable, mutable and has no duplicate elements.
        where as 
        dictionary is collection of key value pairs used to store and retrieve data using unique keys.
        example 
                set                           dict
        myset = {1,2,3}                    mydict = {"a":1,"c":2}
How is a dictionary different from a list?
        A list is an ordered collection of items that are accessed using their index position, 
        where as,
        dictionary is an ordered collection of key-value pairs that are accessed using unique keys. 
                
What is the difference between Python Arrays and Lists?
        Both are used to store sequential data but differe in their own way
        Arrays will store the data of same type
        where as 
        lists can store multiple types of data

What is pass in Python?
        In python, pass acts like syntactic placeholder where it does absolutely nothing.
        Because Python relies on indentation to define code blocks, you cannot leave a block empty. 

What are *args and **kwargs?
        *args is used to pass a variable number of arguments.
                eg:
                def fun(*argv):
                    for arg in argv:
                        print(arg)
                
                fun('Hello', 'Welcome', 'to', 'GeeksforGeeks')
        where as 
        **kwags is used to accept a variable number of keyword arguments to a function. these are collected internally as dictonary
                eg:
                def func(**kwargs):
                        for k,V in kwargs.items():
                                print(k,v)
                fun(s1="lahari",s2="priya",s3="varada")

what is the use of self in python ?
        self argument always refer to the instance of class. with this keyword, you can access or modify the class attributes and methods.
        it is always passed as a first parameter.

what is the use of__init__ ?
        __init__ is automatically triggered and allocates a memory whenever a new object is created. The purpose is to initalize the object attributes with the custom values. Without init method, you need to assign the values manually.

What is break, continue and pass in Python?
        break terminates the current execution .
        continue it skips the current execution and continues iteration for the remaining set
        pass it is used to fill the empty block it does nothing while execution.

What is docstring in Python?
        It is a multi line string used to document a specific code segment
        it should describe what the function is doing 
                def add(a, b):
                    """Return the sum of two numbers."""
                    return a + b
                print(add.__doc__)

How can you concatenate two lists in Python?
        any of the following methods
        a = [1,2,3] b= [4,5,6]
        1. a+b
        2. a.extends(b)

What is the difference between append() and extend()?
        append() will attach the items as a singleobject
        extend() will flatten the list 

What is the difference between remove(), pop(), and del?
        removes()  remove the first matching element 
        pop() removes at specified index
        del removes the element or enter list

What is the purpose of if __name__ == "__main__" in Python?
        It ensures file is  executed directly with out importing a module

What is slicing in Python?
        it is used to extract some part from a sequence such as string or lists
        a[1:3],a[1:],a[::-1]

What is the difference between a shallow copy and a deep copy?
        shallow copy: 
                it copies the toplevel of object meaning the inner levels will still pointd the original object memory references
        where as 
        deep cody copies and creates the new memory refrences.

What are Magic (Dunder) Methods in Python?
        these have special methods whose names starts and ends with --. you dont have to call these methods directly. 
        python calls them for you automatically in certain situations like when an object is created, printed or compared to another objet
        eg:
                __init__
                __equals__
                __str__
                __len__
        class Student:
        
            def __init__(self, name):
                self.name = name
        
            def __str__(self):
                return self.name
        
        s = Student("Alice")
        print(s)

What is the purpose of the super() function in Python?
        super() is used to allows access the parent class constructor and methods.
        eg of inheritance:
                class Animal:
                    def __init__(self):
                        print("Animal")
                class Dog(Animal):
                    def __init__(self):
                        super().__init__()
                        print("Dog")
                Dog()
 What is Polymorphism in Python?
         It allows same method name to perform different actions depending on the object that calls it.
        example
                Dog and Cat override the sound() method of Animal.
                The same sound() method produces different outputs for different objects.
                This demonstrates polymorphism in Python.

What is the use of help() and dir() functions?
        help() is used to display the documentation of modules, classes, methods
        dir() is used to display the attributes and methods available in the class or modules

How will you check if a class is a child of another class?
        isSubClass()
        isInstanceOf()

How do you create and use a virtual environment?
        you can create a virtual environment
                python3 -m venv env
        you can activate by navigatiing to the folder src/env/bin and the run activate command

What are Access Specifiers in Python?
        there are 3 types of access specifiers in python.
        public these can be accessed any where
        protected these are intended to accessed only by class members or subclass members . these are declared with _
        private these should  be strictly accessed by class members only. direct access can be done by name mangling.

What is the difference between @classmethod, @staticmethod and instance methods in Python?
        @instancemethod: Operates on an instance of a class and has access to instance attributes. Always takes self as fist parameter
        @classmethod: works with class and takes cls as first parameter. it is defined using @classmethod
        @staticmethod: doesnot access class or instances attributes. mainly used for utility functions.
        class and static methods can be access used className. No Object creation is required.

what are decorator ?
        decorators it tells the  how the function or class would behave with out changing the source code.

How memory management is done in python
        All objects and data structures are stored in a private heap.
        The Python interpreter manages memory allocation automatically.
        Programmers do not access the private heap directly.
        Python uses garbage collection to free unused memory.

What are Pickling and Unpickling?
        pickling : the process of converting python object to bytestream. this can be used stored in a file and used to send over network. this can be done using pickle.dump()
        unpickling: the process of retrieving byte stream to python object. this can be done using pickle.load()

What is a context manager in Python?
        A context manager is used to manage resources like files, database connections, or locks.
        It makes sure things are properly opened and closed.You use it with the with keyword.
        It runs setup code when entering and cleanup code when exiting.
        This avoids bugs like forgetting to close a file.It keeps code clean and safe.

What is the difference between dict.get() and indexing dict[key]?
        Using dict[key] throws an error if the key is missing, while dict.get(key) returns None or a default value, this is useful when you are not sure if a key exists, so get() is safer in scripts, while indexing is good when the key must be present.
        d = {"a": 1}
        print(d.get("b"))        # None
        print(d.get("b", 0))     # 0
        # print(d["b"])          # KeyErro
